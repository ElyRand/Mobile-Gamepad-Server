<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      Material 3, because the layouts use M3 text appearances
      (textAppearanceHeadlineSmall and friends). Under a Material 2 theme
      those attributes do not resolve and every styled TextView silently
      falls back to a tiny default.
      DayNight handles the dark variant, so no values-night override.
    -->
    <style name="Theme.MobileGamepad" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_40</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>
</resources>
